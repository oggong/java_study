
(1) 변수 초기화
	int a; // 변수 선언(메모리 확보)
	a =10; // 값지정
	
	int b = 20; // 변수 초기화
	
(2) 배열 초기화

	int []a; // 변수 선언
	a = new int[3]; // 객체 생성(메모리 확보)
	a[0] = 10; // 값 지정	
	a[1] = 20; // 값 지정
	a[2] = 30; // 값 지정
	
	int b[] = {10,20,30} // 배열 초기화 (변수 선언 , 객체 생성 , 메모리확보 한번에 함)
	
(3)	클래스 초기화
	Student s; // 변수 선언
	s = new Student(); // 객체 생성(메모리 확보)
	s.name = "홍길동"; // 값 지정
	
	Student s = new Student("홍길동", 100); 
	
	